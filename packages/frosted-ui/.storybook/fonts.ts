// Fonts for storybook's own chrome (sidebar, toolbar, docs pages).
// Components get their font from --default-font-family in tokens/typography.css.
export const fontBase = `-apple-system, BlinkMacSystemFont, 'SF Pro Text', 'SF Pro Display', system-ui, sans-serif`;
export const fontCode = `'SF Mono', ui-monospace, Menlo, monospace`;
