import { Controls, Meta, Primary, Stories } from '@storybook/addon-docs/blocks';

import * as SwitchStories from '../../../src/components/switch/switch.stories';
import { Demo } from '../../components/demo';
import { PropsTable } from '../../components/props-table';

<Meta of={SwitchStories} />

# Switch

A toggle for on/off state.

<Demo id="switch" />

## Usage

```tsx
import { Switch } from '@aussieljk/frosted';

<Switch defaultChecked color="orange" />;
```

Supports controlled usage via `checked` / `onCheckedChange`, plus `defaultChecked` and
`disabled`.

## Props

<PropsTable component="Switch" />

## Playground

<Primary />

<Controls />

## Stories

<Stories includePrimary={false} />
