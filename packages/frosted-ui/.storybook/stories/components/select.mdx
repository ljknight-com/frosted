import { Controls, Meta, Primary, Stories } from '@storybook/addon-docs/blocks';

import * as SelectStories from '../../../src/components/select/select.stories';
import { Demo } from '../../components/demo';
import { PropsTable } from '../../components/props-table';

<Meta of={SelectStories} />

# Select

A dropdown for picking a value from a list.

<Demo id="select" />

## Usage

Select is a compound component:

```tsx
import { Select } from '@aussieljk/frosted';

<Select.Root defaultValue="apple" size="2">
  <Select.Trigger />
  <Select.Content>
    <Select.Group>
      <Select.GroupLabel>Fruits</Select.GroupLabel>
      <Select.Item value="apple">Apple</Select.Item>
      <Select.Item value="orange">Orange</Select.Item>
    </Select.Group>
    <Select.Separator />
    <Select.Item value="carrot">Carrot</Select.Item>
  </Select.Content>
</Select.Root>;
```

## API reference

### Select.Root

State container for the select.

<PropsTable component="Select.Root" />

### Select.Trigger

The button that opens the menu.

<PropsTable component="Select.Trigger" />

### Select.Content

The popup listbox.

<PropsTable component="Select.Content" />

### Select.Group and Select.GroupLabel

`Select.Group` groups items and `Select.GroupLabel` labels the group. Both take only standard
element props.

### Select.Item

A selectable option.

<PropsTable component="Select.Item" />

### Select.Separator

Visual divider between items or groups.

<PropsTable component="Select.Separator" />

## Playground

<Primary />

<Controls />

## Stories

<Stories includePrimary={false} />
