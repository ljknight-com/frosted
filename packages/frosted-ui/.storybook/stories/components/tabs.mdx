import { Controls, Meta, Primary, Stories } from '@storybook/addon-docs/blocks';

import * as TabsStories from '../../../src/components/tabs/tabs.stories';
import { Demo } from '../../components/demo';
import { PropsTable } from '../../components/props-table';

<Meta of={TabsStories} />

# Tabs

Switch between sections of related content.

<Demo id="tabs" />

## Usage

```tsx
import { Tabs } from '@aussieljk/frosted';

<Tabs.Root defaultValue="account">
  <Tabs.List>
    <Tabs.Trigger value="account">Account</Tabs.Trigger>
    <Tabs.Trigger value="settings">Settings</Tabs.Trigger>
  </Tabs.List>
  <Tabs.Content value="account">…</Tabs.Content>
  <Tabs.Content value="settings">…</Tabs.Content>
</Tabs.Root>;
```

## API reference

### Tabs.Root

State container for the tabs.

<PropsTable component="Tabs.Root" />

### Tabs.List

Row of triggers.

<PropsTable component="Tabs.List" />

### Tabs.Trigger

Activates the tab with the matching `value`.

<PropsTable component="Tabs.Trigger" />

### Tabs.Content

Panel shown for the matching `value`.

<PropsTable component="Tabs.Content" />

## Related

`TabsNav` for link-based (routing) tab bars and `SegmentedControl` for a filled toggle
group.

## Playground

<Primary />

<Controls />

## Stories

<Stories includePrimary={false} />
