import { Controls, Meta, Primary, Stories } from '@storybook/addon-docs/blocks';

import * as SegmentedControlStories from '../../../src/components/segmented-control/segmented-control.stories';
import { Demo } from '../../components/demo';
import { PropsTable } from '../../components/props-table';

<Meta of={SegmentedControlStories} />

# SegmentedControl

Toggle between views with a filled segmented button group.

<Demo id="segmented-control" />

## Usage

```tsx
import { SegmentedControl } from '@aussieljk/frosted';

<SegmentedControl.Root defaultValue="account">
  <SegmentedControl.List>
    <SegmentedControl.Trigger value="account">Account</SegmentedControl.Trigger>
    <SegmentedControl.Trigger value="settings">Settings</SegmentedControl.Trigger>
  </SegmentedControl.List>
  <SegmentedControl.Content value="account">…</SegmentedControl.Content>
  <SegmentedControl.Content value="settings">…</SegmentedControl.Content>
</SegmentedControl.Root>;
```

## API reference

### SegmentedControl.Root

State container for the segments.

<PropsTable component="SegmentedControl.Root" />

### SegmentedControl.List

Row of triggers.

<PropsTable component="SegmentedControl.List" />

### SegmentedControl.Trigger

Activates the segment with the matching `value`.

<PropsTable component="SegmentedControl.Trigger" />

### SegmentedControl.Content

Panel shown for the matching `value`.

<PropsTable component="SegmentedControl.Content" />

## Related

`Tabs` for underlined in-page tabs, `SegmentedControlNav` for link-based segments, and
`SegmentedControlRadioGroup` for a form-friendly single-choice control.

## Playground

<Primary />

<Controls />

## Stories

<Stories includePrimary={false} />
